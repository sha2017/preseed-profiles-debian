# preseed-profiles-debian

This repository contains the preseed files used on PXE and USB keys for sha2017 deployment of bare metal clients, servers, servers with raid and other debian based system. 

